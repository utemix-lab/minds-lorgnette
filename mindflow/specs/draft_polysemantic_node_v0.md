# Черновик спецификации: Полисемантический узел (Polysemantic Node)

**Версия:** 0.1 (экспериментальная, основана на инсайте OM)
**Источник:** Эксперимент «Принцип Римана для феномена OM» (2025-01-08)

## 1. Назначение
Спецификация описывает структуру данных и протокол поведения для концепта, который может иметь множество различных, контекстно-зависимых интерпретаций (как узел `OM` с его расшифровками).

## 2. Абстрактная модель (основана на трёх правилах)
PolysemanticNode:

id: String # Правило 1: Уникальный идентификатор контейнера (напр., "OM")

interpretations: List[Interpretation] # Правило 2: Множество возможных интерпретаций

activeInterpretation: Interpretation? # Текущая активная интерпретация (вычисляется)

Interpretation:

id: String # Уникальный ключ интерпретации (напр., "origin_method")

label: String # Человекочитаемое название

description: String # Разъяснение смысла

contextRules: ContextRule[] # Правило 3: Условия активации в зависимости от контекста

Context:

fields: Map<String, Any> # Произвольный набор ключ-значение, описывающий текущую ситуацию
# Примеры полей: {"project": "vova-i-petrova", "userRole": "explorer", "mode": "writing"}

text

## 3. Протокол поведения (псевдокод)
function getActiveInterpretation(node: PolysemanticNode, currentContext: Context): Interpretation {
// 1. Фильтрация: Выбрать все интерпретации, чьи contextRules удовлетворяют currentContext
let candidates = node.interpretations.filter(interp =>
interp.contextRules.some(rule => rule.isSatisfiedBy(currentContext))
);

text
// 2. Разрешение неоднозначностей: Если кандидатов несколько, применить приоритет (простейшая логика — первое совпадение)
// (В будущем это может быть более сложный драйвер разрешения конфликтов)
return candidates[0] || node.interpretations[0] || null; // Возвращаем первую подходящую, первую в списке или null
}

text

## 4. Пример конкретизации для YAML/JSON

```yaml
# Конкретный пример узла OM в формате, близком к будущей конфигурации
id: OM
interpretations:
  - id: origin_method
    label: Origin Method
    description: Маршрут через истоки и интеграцию опыта.
    context_rules:
      - field: project
        operator: equals
        value: minds-lorgnette
      - field: intent
        operator: equals
        value: methodology
  - id: ohmic_mode
    label: Ohmic Mode
    description: Маршрут через сопротивление и провалы.
    context_rules:
      - field: project
        operator: equals
        value: extended-mind
      - field: mode
        operator: equals
        value: reflection
5. Открытые вопросы (направления для следующего эксперимента)
Q1: Как формализовать ContextRule? Достаточно ли простых операторов (equals, contains), или нужна логика (and, or)?

Q2: Какой механизм разрешения конфликтов, если несколько интерпретаций подходят под контекст? Приоритеты? Веса? Вовлечение пользователя?

Q3: Может ли одна интерпретация иметь несколько наборов contextRules для разных ситуаций?

6. Связи с проектами
Minds Lorgnette: Эта спецификация — кандидат на формат конфигурации для драйвера онтологии нового типа.

Extended-mind: Такие узлы могут быть элементами его нормативного слоя.

OM-репозиторий: Фактически является ручной, статической реализацией этой спецификации.

text

### 🎯 Почему это правильный следующий шаг вместо кода?

1.  **Снимает дилемму:** Мы не пишем код «в вакууме». Мы **проектируем формальный язык описания** для нашего инсайта. Это работа на уровне *мета*.
2.  **Проверяет силу правил:** Если три простых правила действительно фундаментальны, они должны без насилия превратиться в строгую спецификацию. Если нет — мы найдём слабое место.
3.  **Создаёт мост к коду:** Следующим шагом после этой спецификации может быть: «Написать парсер, который читает YAML-файл в структуру `PolysemanticNode`». Это уже чёткая, крошечная техническая задача.
4.  **Является активом:** Такой черновик спецификации — это уже **ценный результат**, который можно показать в грантовой заявке как пример методологии и глубины проработки.

**Итак, наш ответ на вопрос «Что дальше?»:** Перейти от инсайта и абстрактных правил к **формальной спецификации первого архитектурного примитива** — полисемантического узла. Это будет конкретный, проверяемый и полезный артефакт.
